< Window x: Class = "EmployeeManager.Views.ChatWindow"
        xmlns = "http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns: x = "http://schemas.microsoft.com/winfx/2006/xaml"
        Title = "Чат отдела" Height = "400" Width = "500" >
    < Grid Margin = "10" >
        < Grid.RowDefinitions >
            < RowDefinition Height = "*" />
            < RowDefinition Height = "Auto" />
        </ Grid.RowDefinitions >

        < ListBox Grid.Row = "0" ItemsSource = "{Binding Messages}" >
            < ListBox.ItemTemplate >
                < DataTemplate >
                    < StackPanel Margin = "5" >
                        < TextBlock FontWeight = "Bold" >
                            < Run Text = "{Binding Sender}" />
                            < Run Text = " (" />
                            < Run Text = "{Binding Department}" />
                            < Run Text = ") " />
                            < Run Text = "{Binding Timestamp, StringFormat=HH:mm:ss}" />
                        </ TextBlock >
                        < TextBlock Text = "{Binding Message}" TextWrapping = "Wrap" />
                    </ StackPanel >
                </ DataTemplate >
            </ ListBox.ItemTemplate >
        </ ListBox >

        < Grid Grid.Row = "1" >
            < Grid.ColumnDefinitions >
                < ColumnDefinition Width = "*" />
                < ColumnDefinition Width = "Auto" />
            </ Grid.ColumnDefinitions >
            < TextBox Text = "{Binding MessageText, UpdateSourceTrigger=PropertyChanged}" Grid.Column = "0" Margin = "0,0,5,0" />
            < Button Content = "Отправить" Command = "{Binding SendCommand}" Grid.Column = "1" Width = "80" />
        </ Grid >
    </ Grid >
</ Window >